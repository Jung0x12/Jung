import { Bebas_Neue, Varela } from "next/font/google";

export const bebasNeueFont = Bebas_Neue({
  weight: "400",
  variable: "--bebas-neue",
});

export const varelaFont = Varela({
  weight: "400",
  variable: "--varela",
});
